// UI ornaments (stars, glows, background decorations) shared across
// Android/Web. Empty for now — none have been extracted as standalone
// assets yet; existing decorations live inline in component CSS.
export {};
