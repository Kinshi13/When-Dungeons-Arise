/** Prepared structure for a future parallax decoration layer (ornaments, stars). Not implemented yet — empty props by design. */
export type DecorationLayerProps = Record<string, never>;

export function DecorationLayer(_props: DecorationLayerProps) {
  return null;
}
