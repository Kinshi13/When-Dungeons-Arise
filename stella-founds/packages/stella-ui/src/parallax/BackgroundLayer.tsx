/**
 * Prepared structure for a future layered parallax background (Web Fase 3+).
 * Intentionally unimplemented — StellaParallaxBackground remains the one
 * background actually rendered today. Empty props by design.
 */
export type BackgroundLayerProps = Record<string, never>;

export function BackgroundLayer(_props: BackgroundLayerProps) {
  return null;
}
