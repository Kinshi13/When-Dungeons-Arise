import { createBrowserRouter, type RouteObject } from 'react-router-dom';

// Placeholder route table — pages will be added in a future phase.
const routes: RouteObject[] = [];

export const router = createBrowserRouter(routes);
