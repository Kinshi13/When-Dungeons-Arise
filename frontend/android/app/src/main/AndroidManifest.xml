<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Necessária pro app buscar dados de rede (clima, cotação de moedas) tanto na WebView
         quanto no fetch nativo em background do widget de Câmbio. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>

        <!-- Widget de tela inicial com os próximos lembretes. Só informativo: tocar abre o app. -->
        <receiver
            android:name=".ReminderWidgetProvider"
            android:exported="false"
            android:label="@string/widget_reminders_title">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_reminders_info" />
        </receiver>

        <!-- Widget de tela inicial com o mini calendário do mês. Só informativo: tocar abre o app. -->
        <receiver
            android:name=".CalendarWidgetProvider"
            android:exported="false"
            android:label="@string/widget_calendar_title">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_calendar_info" />
        </receiver>

        <!-- Widget de tela inicial com a cotação de uma moeda (par escolhido nas configurações
             do app). Só informativo: tocar abre o app. Busca a cotação direto da AwesomeAPI em
             background, tanto no ciclo automático do próprio widget quanto quando o app avisa
             que o par mudou. -->
        <receiver
            android:name=".CurrencyWidgetProvider"
            android:exported="false"
            android:label="@string/widget_currency_title">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_currency_info" />
        </receiver>

        <!-- Leitor de notificações do sistema (gerenciador de notificações do Mural).
             Só ativa depois que o usuário liga manualmente o acesso em Ajustes > Apps >
             Acesso especial > Acesso a notificações. -->
        <service
            android:name=".AppNotificationListenerService"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="false">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>
    </application>

</manifest>
